<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | CSE Motors</title> <!-- EJS tag for dynamic title -->
    <link rel="stylesheet" href="/css/styles.css" media="screen">
  </head>
  <body>
    <div id="wrapper">
      <header id="top-header">
        <span class="siteName">
          <a href="/" title="Return to home page">CSE Motors</a>
        </span>

        <div id="tools">
          <% if (locals.loggedin) { %>
            <span class="welcome">WELCOME <%= locals.accountData.account_firstname.toUpperCase() %></span>
            <a href="/account/logout" class="logout">LOGOUT</a>
          <% } else { %>
            <a title="Click to log in" href="/account/login">MY ACCOUNT</a>
          <% } %>
        </div>

      </header>
      
      <nav>
        <%- nav %>
      </nav>
  
      <!-- Flash Messages -->
      <% if (messages()) { %>
        <div id="messages">
          <ul class="messages">
            <% if (messages('notice')) { %>
              <li class="notice"><%= messages('notice') %></li>
            <% } %>
            <% if (messages('success')) { %>
              <li class="success"><%= messages('success') %></li>
            <% } %>
            <% if (messages('error')) { %>
              <li class="error"><%= messages('error') %></li>
            <% } %>
          </ul>
        </div>
      <% } %>
      
      <!-- EJS content injection -->
      <%- body %> <!-- This will render the content from other EJS files -->

      <footer>
        <%- include("../partials/footer") %> <!-- Include the footer partial -->
      </footer>
      
    </div>
  </body>
</html>
